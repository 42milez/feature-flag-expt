apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: feature-flag-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - name: web
      port: 3000
      targetPort: web
