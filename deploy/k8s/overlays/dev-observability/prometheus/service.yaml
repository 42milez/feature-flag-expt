apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: feature-flag-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: web
      port: 9090
      targetPort: web
