interface:
  display_name: "Create GitHub PR"
  short_description: "Publish local repo changes as a safe draft GitHub pull request."
  default_prompt: "Use $github-create-pr to create a draft GitHub PR for the current changes."

policy:
  allow_implicit_invocation: false
