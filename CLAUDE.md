# Project Instructions

- All written content — source code comments, documentation (README.md, etc.), and any other text artifacts — must be in English.
- After completing any implementation task, always run the full test suite with `./gradlew :service:test` and confirm all tests pass before reporting the task as done.
